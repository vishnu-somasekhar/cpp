#include <iostream>
#include <string>
#include <vector>

using namespace std;

int anagram(string s1, string s2)
{
    vector<int> arr;

    int iValue = s1.compare(s2) == 0? 0 : -1;

    for(int i = 0; s1[i] && s2[i]; i++) {

        arr[i]

    }

}

int main()
{
    return 0;
}